//An enum to determine the rotation of the ship.  
//The directions describe which way the ship's bow is facing.
//Using a randon number generator and assigning a value of this enum will give a random direction.
public enum ShipOrientation
{
    UP = 0,
    LEFT = 90,
    DOWN = 180,
    RIGHT = -90
};